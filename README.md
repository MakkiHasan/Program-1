# Simple Webpage with Heading and Paragraph

This is a very basic HTML and CSS program that displays a styled heading and paragraph.

## 🧠 Features
- Uses **internal CSS** within the `<style>` tag.
- Sets a **light background color**.
- Styles text with different **colors and font sizes**.

## 💻 How to Run
1. Save the file as `index.html`.
2. Open it in your browser (e.g., Chrome, Firefox).
3. You’ll see a blue heading and green paragraph on a light background.

## 🧩 Technologies Used
- HTML
- CSS (internal style)
